---
export interface Props {
  href?: string;
  variant?: "primary" | "secondary" | "accent" | "outline";
  size?: "sm" | "md" | "lg";
  class?: string;
  target?: string;
  rel?: string;
  disabled?: boolean;
  type?: "button" | "submit" | "reset";
  ariaLabel?: string;
}

const {
  href,
  variant = "primary",
  size = "md",
  class: className = "",
  target,
  rel,
  disabled = false,
  type = "button",
  ariaLabel,
  ...rest
} = Astro.props;

const baseStyles = [
  "inline-flex items-center justify-center gap-2",
  "font-sans text-base font-medium",
  "border-2 rounded-lg",
  "transition-all duration-300 ease-in-out",
  "focus:outline-none focus:ring-2 focus:ring-offset-2",
  "disabled:opacity-50 disabled:cursor-not-allowed",
  "min-h-[44px] min-w-[44px]", // Mobile touch target
  "transform hover:scale-105 active:scale-95",
];

const sizeStyles = {
  sm: "px-4 py-2 text-sm",
  md: "px-6 py-3",
  lg: "px-8 py-4 text-lg",
};

const variantStyles = {
  primary: [
    "bg-gradient-to-r from-primary-600 to-primary-700 text-white border-primary-600",
    "hover:from-primary-700 hover:to-primary-800 hover:border-primary-700",
    "active:from-primary-800 active:to-primary-900",
    "focus:ring-primary-500",
    "shadow-lg hover:shadow-xl",
  ].join(" "),
  secondary: [
    "bg-gradient-to-r from-secondary-600 to-secondary-700 text-white border-secondary-600",
    "hover:from-secondary-700 hover:to-secondary-800 hover:border-secondary-700",
    "active:from-secondary-800 active:to-secondary-900",
    "focus:ring-secondary-500",
    "shadow-lg hover:shadow-xl",
  ].join(" "),
  accent: [
    "bg-gradient-to-r from-accent-500 to-accent-600 text-white border-accent-500",
    "hover:from-accent-600 hover:to-accent-700 hover:border-accent-600",
    "active:from-accent-700 active:to-accent-800",
    "focus:ring-accent-500",
    "shadow-lg hover:shadow-xl",
  ].join(" "),
  outline: [
    "bg-white text-gray-800 border-gray-300",
    "hover:bg-gray-50 hover:border-gray-400",
    "active:bg-gray-100",
    "focus:ring-primary-500",
    "shadow-md hover:shadow-lg",
  ].join(" "),
};

const classes = [
  ...baseStyles,
  sizeStyles[size],
  variantStyles[variant],
  className,
].join(" ");

const Element = href ? "a" : "button";
---

<Element
  href={href}
  class={classes}
  target={target}
  rel={target === "_blank" ? "noopener noreferrer" : rel}
  disabled={disabled}
  type={!href ? type : undefined}
  aria-label={ariaLabel}
  role={!href ? "button" : undefined}
  {...rest}
>
  <slot />
</Element>

<style>
  /* Ensure proper touch target size on mobile */
  @media (max-width: 640px) {
    a,
    button {
      min-height: 44px;
      min-width: 44px;
    }
  }
</style>
